from setuptools import setup, find_packages
 
setup(     
     name='tenant_users',    # This is the name of your PyPI-package.
     version='0.2',                          # Update the version number for new releases
     packages = find_packages()                 # The name of your scipt, and also the command you'll be using for calling it
 )

